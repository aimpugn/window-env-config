alias g = git
alias pgb = prune-gone-branches

# ls
alias ll = ls -l

# save
alias savef = save --force
alias savea = save --append

# copy and paste
alias pbcopy = clip
alias pbpaste = powershell -c Get-Clipboard
